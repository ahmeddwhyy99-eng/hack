RP_ORIGIN=http://localhost:4000
AUTH_ORIGIN=http://localhost:3001
AUTH_CLIENT_ID=memberspace
# Same random client secret as the provider. Never expose in browser code.
AUTH_CLIENT_SECRET=
# The portal needs no database credentials or service-role key.
